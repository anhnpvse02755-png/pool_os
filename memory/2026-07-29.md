# Pool OS Product Decisions - 2026-07-29

## FEATURE_012 Equipment Comparison v2 — Closed

PO Final Review approved with minor documentation corrections. No code
changes. SHA 83fdd0b.

Implementation, accepted as-is:
- v2 supersedes v1; v1 had max-2 cap + FIFO eviction.
- v2 uses `Set<int>` selection (unbounded, no evict).
- `Compare (N)` button triggers `Navigator.push(MaterialPageRoute(
  EquipmentComparisonScreen(...)))`. No GoRouter route.
- EquipmentComparisonScreen renders a multi-column table
  (current implementation uses `DataTable`) inside vertical +
  horizontal `SingleChildScrollView`.
- Insufficient data → literal `Chưa đủ dữ liệu.` per cue.
- No new repository / projection / Drift table / migration / service.
- EquipmentPerformanceProjection unmodified.

Documentation corrections (PO Final Review):
- Removed "future workflow" / "reusable comparison view foundation" /
  "Comparison Platform / Engine / Framework" architectural framing.
- Reframed DataTable as implementation detail (line 30 of report v3):
  "Table implementation is an implementation detail and may change
  without affecting the Product Specification."
- Outstanding #2: "Current implementation keeps
  `EquipmentComparisonSection` in the codebase because it has not been
  removed during this revision. Future cleanup is optional."
- Audit caveat #10: "retained but currently unused by FEATURE_012".

PO Decision:
- Product Design: PASS
- UX: PASS
- Business Rule: PASS
- Scope: PASS
- Architecture: PASS
- Beta Ready: YES

## Roadmap V3 (Beta) frozen

PO froze the roadmap at SHA 1047e25. After FEATURE_001..FEATURE_012
(all closed/authorized), subsequent work is Epic-scoped only:

- EPIC 01 — Match Engine
- EPIC 02 — Statistics & Analytics
- EPIC 03 — Training System
- EPIC 04 — League & Tournament
- EPIC 05 — Knowledge System
- EPIC 06 — AI Coach
- EPIC 07 — Community
- EPIC 08 — Marketplace
- EPIC 09 — Administration

Beta release requires FEATURE_001..FEATURE_012 + EPIC 01 + EPIC 02 +
EPIC 03. Each Epic is reviewed at Epic boundary, not at every
internal task. Roadmap V3 file:
`architecture/product/POOL_OS_ROADMAP_V3_BETA.md`. Old roadmap file
`architecture/product/POOL_OS_GUIDED_LEARNING_EQUIPMENT_AI_ROADMAP.md`
preserved for historical reference.

## PO escalation lesson (durable)

PO confirmed 2026-07-29 Product Review comment, applied to future
FEATURE Engineering Reports:

- Reports describe **current state only**. No "future workflow", no
  "reusable comparison view foundation", no "Comparison Platform /
  Engine / Framework" framing.
- DataTable / specific widget references are implementation detail.
- Widgets retained in the codebase are "retained because not removed
  in this revision" — nothing more.

## Workflow state

PO_HANDOFF.md updated:
- `active_feature: FEATURE_012`
- `workflow_state: accepted_closed`
- `next_action: standby for EPIC 01 — Match Engine`

MEMORY.md appended with the two durable facts above.

## Cross-machine sync

All 4 close commits pushed to `origin/master`:

```
77ed046  feat(product): MEMORY update — FEATURE_012 closed + Roadmap V3 frozen
8c59f01  feat(product): close FEATURE_012 Equipment Comparison v2
83fdd0b  docs(FEATURE_012): PO Final Review corrections — close FEATURE_012
1047e25  docs(roadmap): freeze Product Roadmap V3 (Beta)
```

Local master = origin master. Working tree clean.
