# Pool OS Product Decisions - 2026-07-25

## Accepted PO Context Sync

GitHub-tracked Markdown is the recoverable Product Owner context shared between
home and office. `architecture/product/PO_HANDOFF.md` owns the current workflow
state, location leases, last decision, and one exact next action;
`architecture/product/PO_BOOTSTRAP.md` defines clean-checkout recovery and
claim. Roadmap and FEATURE specifications remain the product contracts, while
`MEMORY.md` retains only durable accepted decisions. Semantic memory is an
optional local index and chat/task history is not authoritative.

## Resolved Baseline Contract

`baseline_commit` is the full HEAD immediately before a Handoff checkpoint or
claim commit is authored. It cannot be the SHA of the commit containing the
Handoff because that would be a circular self-reference. A receiver verifies
that the baseline is an ancestor of checked-out HEAD, local HEAD equals the
remote-tracking HEAD, the Handoff is tracked at HEAD, and every post-baseline
commit and path matches the authorized checkpoint or claim scope. A claim
records the verified pre-claim HEAD as its new baseline. Post-checkpoint HEAD is
never required to equal `baseline_commit`.

## Current FEATURE_004 State

- Branch and verified pre-start baseline: `product/guided-learning-pilot` at
  `e598d136e434507d0b03379418b548dd0ffea033`.
- Workflow: `engineering`.
- FEATURE_004: `Accepted; Implementation Authorized`.
- Engineering location/status: existing home Code task, `implementing`.
- FEATURE_004 Engineering Report: `none`.
- FEATURE_004 WIP observed at milestone start: `none`.
- PO Context Sync Engineering evidence: accepted; the changes-request blocker
  was resolved and Product Owner authorized repository closure.
- Priority decision: PO Context Sync is Accepted and Closed; FEATURE_004
  implementation has started under its accepted specification.

## Outstanding Engineering Action

Product Owner accepted the FEATURE_004 Engineering Report. The exact next
action is: existing home Code task commits and pushes exactly the accepted
FEATURE_004 implementation, then reports the full SHA. FEATURE_005 and later
implementation remain unauthorized.

## FEATURE_004 Review And Closure Authorization

Engineering completed the accepted Active Player lifecycle without commit or
push. Product Owner review accepted the deterministic v29 migration, partial
unique index, exact-one repository lifecycle, stable failures, rollback,
post-commit revision handoff, and refresh hooks for Player Profile,
FEATURE_001-003, Dashboard and Statistics.

Accepted evidence: focused lifecycle/migration/handoff `16/16`, final lifecycle
`10/10`, FEATURE_001-003 and Player `48/48`, compatibility `12/12`, full app
`1234/1234`, Knowledge `75/75`, Foundation Freeze `76/76`, Architecture Fitness
`133 existing / 0 new`, analyzer zero errors/warnings, formatter and diff checks
clean.

The authorized closure allowlist is exactly:

- `app/lib/features/player/data/database/app_database.dart`;
- `app/lib/features/player/data/repositories/player_repository.dart`;
- `app/lib/features/player/domain/player_lifecycle_failure.dart`;
- `app/lib/features/player/presentation/player_provider.dart`;
- `app/lib/features/player/presentation/player_profile_provider.dart`;
- `app/lib/features/player/presentation/career_timeline_section.dart`;
- `app/lib/features/player_model/presentation/player_progress_provider.dart`;
- `app/lib/features/equipment/presentation/equipment_provider.dart`;
- `app/lib/features/dashboard/presentation/dashboard_provider.dart`;
- `app/lib/features/statistics/presentation/statistics_provider.dart`;
- `app/test/features/player/active_player_lifecycle_test.dart`;
- `app/test/features/player/active_player_migration_test.dart`;
- `app/test/features/player/active_player_handoff_test.dart`;
- `app/test/daily_readiness_persistence_test.dart`;
- `app/test/task_12_data_center_test.dart`.

## Coordination Constraints

- Only the home PO is active; office may act only after a pushed handoff and a
  verified claim.
- `engineering_location: home` prevents a duplicate FEATURE_004 Code task.
- Uncommitted Code work is local to its worktree and is not visible on another
  machine. No WIP-transfer policy is authorized.
- The 15-file FEATURE_004 WIP is accepted and awaiting its authorized closure
  commit/push on the home worktree.
- The circular-SHA review blocker is resolved through pre-checkpoint ancestry
  and complete post-baseline history/path audit semantics; Product Owner
  accepted the corrected Engineering evidence.
- The matching `PO_CONTEXT_SYNC_SPEC.md` and durable `MEMORY.md` corrections are
  Product Owner-authored authority; Engineering did not modify those decisions.
- No unresolved product question is recorded. The next work is the existing
  authorized home Code task's FEATURE_004 implementation and Engineering Report.

## FEATURE_004 Repository Closure

FEATURE_004 closed at `c9ebde09e3754eb1ba0d4804b0afc97c8ac599d9` with exactly
the accepted 15-file allowlist. Local and remote branch heads matched and the
worktree was clean. No tag, PR, merge, stash or FEATURE_005 implementation was
created.

The active workflow is now FEATURE_005 Product specification planning.
Engineering is idle and no FEATURE_005 implementation is authorized. Product
Owner may derive a draft from the accepted roadmap and repository evidence;
any product choice that cannot be resolved from existing authority must be
recorded for the user rather than invented.

## FEATURE_005 Draft

Product Owner drafted
`architecture/product/features/FEATURE_005_PLAYER_PROFILE_COMPATIBILITY_PROVENANCE.md`
from E3-G01/E3-G03 and live repository evidence. The draft defines a derived,
versioned, fail-closed compatibility snapshot and provenance adapter over the
existing Player row. It preserves integer persistence identity and maps it to
the foundation canonical form without claiming account or cross-device
identity. It excludes Active Player state from profile digests and adds no
schema, cache, UI or write source.

No open product decision was found at draft time. The existing home Code task
is authorized only to audit the specification and return a Product
Specification Report. FEATURE_005 implementation remains unauthorized.

The first audit returned Changes Requested for inconsistent live aliases,
lossy legacy list decoding, incomplete wire/digest rules, calendar-date
ambiguity, no-op semantics and adapter-owned identity strictness. Product Owner
resolved these without new user-facing choices: all live aliases remain
accepted with explicit locale mapping; raw source assessment retains malformed
JSON; `startedPlayingAt` is `YYYY-MM-DD`; no-op stability is read-only; name
whitespace is fail-closed; and wire/failure/diagnostic ordering is explicit.
The existing home Code task is authorized for re-audit only.

## FEATURE_005 Unresolved Digest Decision

The second read-only audit resolved every first-audit issue except one product
choice. Lossless raw assessment conflicts with the existing requirement that
reordered equivalent preference lists reproduce the same `sourceDigest` and
snapshot digest.

The user must choose one policy:

1. `sourceDigest` hashes canonical parsed profile facts, so list order and JSON
   formatting do not change it; add separate `rawAssessmentDigest` when exact
   storage attribution is required.
2. `sourceDigest` hashes exact raw storage, so list order or JSON formatting
   changes both source and snapshot digests; remove list-order invariance.

Exact wire key order and stable failure-code precedence can be specified
mechanically after this choice. FEATURE_005 remains Changes Requested with no
implementation authorization. The blocker was checkpointed at
`2026-07-25T04:10:04Z`; Engineering is idle and the worktree was clean.

At `2026-07-25T04:40:05Z`, no user response had arrived after 30 minutes. The
Code task remained idle, the worktree was clean, and local/remote heads matched.
The blocker and exact next action were checkpointed again before the authorized
safe Windows shutdown. No implementation WIP existed and no Product choice was
inferred.

The user later returned and selected policy 1: `sourceDigest` hashes canonical
parsed profile facts, while a separate `rawAssessmentDigest` hashes exact raw
storage. Canonical snapshot digest remains stable for equivalent list order,
JSON formatting and accepted language aliases; raw assessment preserves storage
differences. Product Owner also locked exact ordered wire tables and stable
failure/diagnostic precedence. FEATURE_005 remains specification-only pending
one final read-only re-audit.

The next audit found one internal contradiction: raw assessment required a
positive `legacyPlayerId` while also requiring `invalid-player-id` diagnostics
for corrupt zero/negative persisted IDs. Product Owner corrected the raw payload
to preserve signed SQLite IDs and raw references; positivity is enforced only
when creating canonical identity. Zero/negative rows remain attributable, have
a stable raw digest, and produce no partial canonical output. No user decision
or scope expansion was required.

Final read-only audit recommended Accepted with no blocker. Product Owner
accepted FEATURE_005 and authorized the existing home Code task to implement
the exact specification. Engineering must return a report without commit/push;
FEATURE_006 remains unauthorized.

Engineering completed FEATURE_005 in exactly five authorized files. Product
Owner reviewed the report and implementation diff and found no blocker. Evidence
passed: focused 22/22, FEATURE_004 16/16, FEATURE_001-003 plus legacy Player
50/50, full app 1256/1256, Knowledge 75/75, Freeze 76/76, Architecture Fitness
133 known/0 new, with analyzer/formatter/diff/scope checks clean. Repository
closure is authorized for the exact five-file allowlist; FEATURE_006 remains
unauthorized.

FEATURE_005 repository closure completed at
`1b38009fddd914965aa41304303108ea6004820f` with local/remote equality and a
clean worktree. PO opened FEATURE_006 planning as Match Identity Compatibility
and Provenance, limited to E4-G01 and a prerequisite read-port slice. Lifecycle,
transactions and Training remain FEATURE_007-009. Engineering is authorized
only for a read-only specification/repository audit.

User established the durable operating roles: this task is Product Owner and
the task named `Code Pool OS` is the persistent Engineering/Coder. PO and Code
must communicate directly and continue each authorized roadmap transition
without waiting for the user to relay messages. Only genuine unresolved Product
choices or new authority are escalated to the user.

FEATURE_006 first audit returned Changes Requested. PO resolved the blockers:
exact raw/canonical wire and digest participation, strict signed/local identity,
nullable legacy Session ID, Unix-second timestamp encoding, schema-conforming
SQLite storage only, exact failure/diagnostic precedence and target adapter
mapping. V1 preserves every evidenced historic game code as distinct and has
no aliases; mismatched SQLite storage class is source-read-failure. Final
read-only re-audit is authorized, not implementation.

FEATURE_006 final re-audit recommended Accepted with no blocker. PO accepted the
locked contract and authorized Code Pool OS to implement the exact five-file
allowlist, run all required evidence and return an Engineering Report without
stage/commit/push. FEATURE_007 remains unauthorized.

Engineering completed FEATURE_006 in its exact five-file allowlist. PO reviewed
the direct report and diff; no blocker remains. Evidence passed focused 19/19,
Match characterization/recording 27/27, FEATURE_004-005 38/38,
FEATURE_001-003/Player 50/50, full app 1275/1275, Knowledge 75/75, Freeze
76/76 and Architecture Fitness 133 known/0 new. Repository closure is
authorized; FEATURE_007 remains unauthorized.

FEATURE_006 repository closure completed at
`80f08ea9f39354047f1892b1dbfd5d3a76697152`, local/remote equal and clean.
PO moved active planning to FEATURE_007 Match Lifecycle State Policy; Code Pool
OS may not implement it until specification audit and PO acceptance.

FEATURE_007 audit returned Changes Requested. PO resolved them without scope
expansion: timestamp lifecycle primitive is distinct from Coordinator-owned
legacy winner metadata in one transaction; command times canonicalize to UTC
Unix seconds; finish on unstarted Match requires supplied start; literal errors,
precedence, conditional writes and generic metadata protection are explicit.
Final read-only re-audit is authorized.

FEATURE_007 final audit recommended Accepted with no blocker. PO accepted the
specification and authorized Code Pool OS to implement the exact lifecycle
policy surfaces and report evidence without stage/commit/push. FEATURE_008
remains unauthorized.

Engineering completed FEATURE_007 in its exact seven-file allowlist. PO
reviewed direct evidence and diff; no blocker remains. Focused 18/18, Match
27/27, FEATURE_004-006 57/57, FEATURE_001-003/Player 50/50, full app 1293/1293,
Knowledge 75/75, Freeze 76/76 and Architecture 133 known/0 new passed.
Repository closure is authorized; FEATURE_008 remains unauthorized.

FEATURE_007 repository closure completed at
`a184bafa3bcd113bc80d57c9448a10d7995cf784` with local/upstream/remote equality
and a clean worktree. FEATURE_008 is now planning only; its specification must
be audited and accepted before Code implementation.

PO authored FEATURE_008 Match Recording Transaction Integrity from roadmap
item E4-G03. The draft locks Session-owned transactional creation, monotonic
per-Session Match numbering, at most one open Match, fail-closed legacy data and
concurrency/rollback evidence. Code Pool OS is authorized only for a read-only
specification and repository audit. Any required schema migration is a blocker
for a separate PO decision; implementation and FEATURE_009 remain unauthorized.

FEATURE_008 first audit returned Changes Requested. PO selected the recommended
prospective no-reuse policy: migration v30 seeds a per-Session high-water mark
from current valid maxima; pre-migration deleted allocations are explicitly not
reconstructable. The revised contract requires a parent write lock before any
read, a durable allocation sidecar, database triggers for number/open-Match
integrity, exact typed failure propagation and deterministic FEATURE_006 digest
change from schema version only. Final read-only re-audit is authorized; no
implementation or FEATURE_009 work is authorized.

FEATURE_008 final re-audit recommended Accepted with no remaining blocker. PO
accepted the specification and authorized Code Pool OS to implement exactly the
v30 allocation/trigger, coordinator, repository, failure mapping and test
allowlist. Engineering must return full evidence without stage/commit/push;
FEATURE_009 remains unauthorized.

FEATURE_008 implementation reached focused 28/28 and Match regression 74/74,
then exposed a scope contradiction in full regression: schema v30 correctly
changes both FEATURE_005 and FEATURE_006 provenance, while the contract named
only FEATURE_006, and one Daily Readiness test pinned schema 29. PO resolved this
without user escalation: both compatibility features must report actual schema
v30; only version-bound digest fields may change. The two stale tests are added
to the allowlist and Code Pool OS must resume the same uncommitted WIP.
