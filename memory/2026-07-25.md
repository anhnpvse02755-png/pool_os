# Pool OS Product Decisions - 2026-07-25

## Accepted PO Context Sync

GitHub-tracked Markdown is the recoverable Product Owner context shared between
home and office. `architecture/product/PO_HANDOFF.md` owns the current workflow
state, location leases, last decision, and one exact next action;
`architecture/product/PO_BOOTSTRAP.md` defines clean-checkout recovery and
claim. Roadmap and FEATURE specifications remain the product contracts, while
`MEMORY.md` retains only durable accepted decisions. Semantic memory is an
optional local index and chat/task history is not authoritative.

## Resolved Baseline Contract

`baseline_commit` is the full HEAD immediately before a Handoff checkpoint or
claim commit is authored. It cannot be the SHA of the commit containing the
Handoff because that would be a circular self-reference. A receiver verifies
that the baseline is an ancestor of checked-out HEAD, local HEAD equals the
remote-tracking HEAD, the Handoff is tracked at HEAD, and every post-baseline
commit and path matches the authorized checkpoint or claim scope. A claim
records the verified pre-claim HEAD as its new baseline. Post-checkpoint HEAD is
never required to equal `baseline_commit`.

## Current FEATURE_004 State

- Branch and verified baseline: `product/guided-learning-pilot` at
  `5348194346c98e0d1cd8a3ce79d0bb8f10716d30` before this milestone's
  uncommitted documentation changes.
- Workflow: `engineering`.
- FEATURE_004: `Accepted; Implementation Authorized`.
- Engineering location/status: existing home Code task, `authorized`.
- FEATURE_004 Engineering Report: `none`.
- FEATURE_004 WIP observed at milestone start: `none`.
- PO Context Sync Engineering evidence: accepted; the changes-request blocker
  was resolved and Product Owner authorized repository closure.
- Priority decision: PO Context Sync is Accepted and Closed; FEATURE_004
  implementation remains authorized.

## Outstanding Engineering Action

The exact next action is: existing authorized home Code task implements
FEATURE_004 and returns an Engineering Report without commit or push.
FEATURE_005 and later work remain unauthorized.

## Coordination Constraints

- Only the home PO is active; office may act only after a pushed handoff and a
  verified claim.
- `engineering_location: home` prevents a duplicate FEATURE_004 Code task.
- Uncommitted Code work is local to its worktree and is not visible on another
  machine. No WIP-transfer policy is authorized.
- The current uncommitted changes are the accepted documentation-only closure
  files, not FEATURE_004 implementation.
- The circular-SHA review blocker is resolved through pre-checkpoint ancestry
  and complete post-baseline history/path audit semantics; Product Owner
  accepted the corrected Engineering evidence.
- The matching `PO_CONTEXT_SYNC_SPEC.md` and durable `MEMORY.md` corrections are
  Product Owner-authored authority; Engineering did not modify those decisions.
- No unresolved product question is recorded. The next work is the existing
  authorized home Code task's FEATURE_004 implementation and Engineering Report.
